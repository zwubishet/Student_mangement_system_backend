table:
  name: students
  schema: public
object_relationships:
  - name: user
    using:
      foreign_key_constraint_on: user_id
select_permissions:
  - role: director
    permission:
      columns:
        - grade_section_id
        - student_id
        - user_id
        - created_at
        - updated_at
      filter: {}
      allow_aggregations: true
  - role: student
    permission:
      columns:
        - grade_section_id
        - student_id
        - user_id
        - created_at
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      allow_aggregations: true
  - role: teacher
    permission:
      columns:
        - grade_section_id
        - student_id
        - user_id
        - created_at
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      allow_aggregations: true
